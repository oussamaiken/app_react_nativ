// Only import react-native-gesture-handler on native platforms
import 'react-native-gesture-handler';

import { createDrawerNavigator, DrawerContentScrollView } from '@react-navigation/drawer';

import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';
import { SafeAreaProvider } from 'react-native-safe-area-context';
import { NavigationContainer } from '@react-navigation/native';
import { Box, extendTheme, HStack, Icon, NativeBaseProvider, VStack } from 'native-base';
import HomeScreen from './Componant/HomeScreen';
import AboutScreen from './Componant/AboutScreen';
import CreatorScreen from './Componant/CreatorScreen';
import { Pressable } from 'react-native-gesture-handler';






export default function App() {
  return (






<AboutScreen/>)
}


